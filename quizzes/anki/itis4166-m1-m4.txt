#separator:tab
#html:true
#columns:Front	Back	Tags
#tags column:3
What are the exact falsy values in JavaScript?	<p style="text-align:left">Seven, plus BigInt zero:</p><pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">false<br>0<br>-0<br>&quot;&quot;   (empty string)<br>null<br>undefined<br>NaN<br>0n</pre>	m1 falsy
Is <code>[]</code> truthy or falsy? <code>{}</code>? <code>"0"</code>? <code>"false"</code>? <code>-1</code>?	<p style="text-align:left"><b>All five are truthy.</b> Every object and every non-empty string is truthy, no matter what it contains. Only the seven falsy values are falsy.</p>	m1 falsy
What does this print, and why is it a bug?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const index = arr.findIndex(p =&gt; p.id === id);<br>if (!index) {<br>  return res.status(404).json({ error: &#x27;not found&#x27; });<br>}</pre>	<p style="text-align:left">Two failures at once:</p><p style="text-align:left">1. <b>Not found returns -1</b>, and <code>-1</code> is <b>truthy</b>, so <code>!index</code> is false and the 404 never fires.</p><p style="text-align:left">2. <b>A real match at index 0</b> gives <code>0</code>, which is <b>falsy</b>, so <code>!index</code> fires on a <i>successful</i> lookup.</p><p style="text-align:left"><b>Fix:</b> <code>if (index === -1)</code>. Always compare an index explicitly.</p>	m1 falsy trace
Why is this validation wrong?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">if (!req.body.quantity) {<br>  return res.status(400).json({ error: &#x27;quantity required&#x27; });<br>}</pre>	<p style="text-align:left">It rejects <code>quantity === 0</code>, which is a legitimate value, because <code>0</code> is falsy. It also rejects <code>""</code> identically to a missing key, hiding the difference.</p><p style="text-align:left"><b>Fix:</b> <code>if (req.body.quantity === undefined)</code>, or check <code>typeof ... !== 'number'</code>.</p>	m1 falsy validation
Why does this always take the true branch?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">// request: GET /api/products?inStock=false<br>if (req.query.inStock) {<br>  // filter to in-stock only<br>}</pre>	<p style="text-align:left"><code>req.query.inStock</code> is the <b>string</b> <code>"false"</code>, not the boolean <code>false</code>. A non-empty string is truthy, so the branch always runs.</p><p style="text-align:left"><b>Fix:</b> <code>if (req.query.inStock === 'true')</code>. Never use <code>Boolean(v)</code> on a query value — <code>Boolean("false") === true</code>.</p>	m1 m4 falsy query
Non-mutating vs mutating array methods — name the members of each group.	<table style="margin:auto"><tr><th>Non-mutating (returns new)</th><th>Mutating (changes original)</th></tr><tr><td><code>.map() .filter() .reduce()<br>.slice() .concat()<br>.find() .findIndex()<br>.toSorted()</code></td><td><code>.push() .pop()<br>.shift() .unshift()<br>.splice() .sort() .reverse()</code></td></tr></table>	m1 arrays
Return value of each: <code>.push()</code>, <code>.pop()</code>, <code>.splice(i,n)</code>, <code>.find()</code>, <code>.findIndex()</code>	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">.push() / .unshift()  -&gt; the new LENGTH (a number)<br>.pop()  / .shift()    -&gt; the REMOVED ELEMENT<br>.splice(i, n)         -&gt; an ARRAY of removed elements<br>                         (always an array, even for one item)<br>.find()               -&gt; the ELEMENT, or undefined<br>.findIndex()          -&gt; the INDEX, or -1</pre>	m1 arrays
What is wrong here?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const prices = [10, 20, 30];<br>prices.map(p =&gt; p * 2);<br>console.log(prices);</pre>	<p style="text-align:left">Prints <code>[10, 20, 30]</code>, unchanged. <code>.map()</code> returns a <b>new</b> array and leaves the original alone; calling it as a bare statement throws the result away.</p><p style="text-align:left"><b>Fix:</b> <code>const doubled = prices.map(p => p * 2);</code></p>	m1 arrays
What is wrong here?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const sorted = arr.push(4);</pre>	<p style="text-align:left"><code>sorted</code> is <b>4</b> — the new length of the array, not the array. This is the mirror image of discarding a <code>.map()</code>: assigning the result of a <i>mutating</i> method usually gives you a number, not data.</p>	m1 arrays
What is wrong here?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const user = users.filter(u =&gt; u.id === id);<br>res.json(user.name);</pre>	<p style="text-align:left"><code>.filter()</code> returns an <b>array</b> (possibly empty), so <code>user.name</code> is <code>undefined</code>.</p><p style="text-align:left"><b>Fix:</b> <code>.find()</code> — it returns the element itself, or <code>undefined</code>.</p>	m1 arrays
<code>slice</code> vs <code>splice</code>: what does each argument mean, what is returned, and which mutates?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">arr.slice(start, end)    // end is an INDEX (exclusive)<br>  - does NOT mutate<br>  - returns a COPY of that range<br><br>arr.splice(start, count) // count is a NUMBER OF ITEMS<br>  - MUTATES arr in place<br>  - returns an ARRAY of what it removed</pre><p style="text-align:left"><code>arr.slice(2, 1)</code> is backwards (end before start) and returns <code>[]</code>.</p>	m1 m4 arrays
Explain line by line:<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const [removed] = posts.splice(index, 1);</pre>	<p style="text-align:left"><b>Right side:</b> <code>posts.splice(index, 1)</code> deletes 1 element from <code>posts</code> <i>in place</i> and returns an array of what it deleted, e.g. <code>[{id:2,...}]</code>.</p><p style="text-align:left"><b>Left side:</b> <code>const [removed] = ...</code> is array destructuring. The brackets are a <i>pattern</i>, not a new array — they mean "bind element 0 to the name <code>removed</code>".</p><p style="text-align:left">Equivalent longhand:</p><pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const result  = posts.splice(index, 1);  // [ {id:2,...} ]<br>const removed = result[0];               //   {id:2,...}</pre>	m1 m4 destructuring
Object destructuring vs array destructuring — what does each match on?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const { name, price } = req.body;  // by KEY NAME<br>const [first, second] = arr;       // by POSITION</pre><p style="text-align:left">Curly braces match property names. Square brackets match index positions. The names in <code>[ ]</code> are yours to choose; the names in <code>{ }</code> must match the keys unless you rename.</p>	m1 destructuring
What do these three destructuring forms do?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const { name = &#x27;Unknown&#x27; } = req.body;<br>const { id: productId } = req.params;<br>const { a } = null;</pre>	<p style="text-align:left">1. <b>Default</b> — applies <i>only</i> when the value is <code>undefined</code>. An empty string or <code>null</code> does not trigger it.</p><p style="text-align:left">2. <b>Rename</b> — reads <code>req.params.id</code>, binds it to <code>productId</code>.</p><p style="text-align:left">3. <b>Throws</b> <code>TypeError</code>. Destructuring a missing <i>property</i> gives <code>undefined</code>, but destructuring <i>from</i> <code>null</code>/<code>undefined</code> throws.</p>	m1 destructuring
Spread vs rest — both are <code>...</code>. What is the difference?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">// SPREAD expands<br>Math.max(...[1,2,3])          // 3   (Math.max([1,2,3]) is NaN)<br>const copy   = { ...original };  // SHALLOW copy<br>const merged = { ...a, ...b };   // later keys win<br><br>// REST collects<br>function f(...args) { }       // args is an array<br>const [head, ...tail] = arr;</pre>	m1 spread
Why is <code>obj.key</code> wrong here?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const key = &#x27;price&#x27;;<br>obj.key<br>obj[key]</pre>	<p style="text-align:left"><code>obj.key</code> looks for a property literally named <code>"key"</code> and gives <code>undefined</code>. <code>obj[key]</code> looks up the property named by the <i>value</i> of <code>key</code>, i.e. <code>obj.price</code>.</p>	m1 objects
<code>var</code> vs <code>let</code> vs <code>const</code> — scope, hoisting, reassignment.	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">var    - FUNCTION-scoped, hoisted and initialized to undefined,<br>         can be redeclared<br>let    - BLOCK-scoped { }, hoisted but in the temporal dead zone<br>const  - BLOCK-scoped, same TDZ, cannot be REASSIGNED</pre><p style="text-align:left"><b>Temporal dead zone (TDZ):</b> the binding exists from the top of the block but reading it before its declaration line throws <code>ReferenceError: Cannot access 'x' before initialization</code>.</p>	m1 scope
<code>const</code> prevents what, exactly?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const arr = [1];<br>arr.push(2);   // ?<br>arr = [2];     // ?</pre>	<p style="text-align:left"><code>arr.push(2)</code> is <b>legal</b>. <code>arr = [2]</code> throws <code>TypeError: Assignment to constant variable.</code></p><p style="text-align:left"><code>const</code> freezes the <b>binding</b> (which value the name points at), never the value itself. Mutating the object or array is always allowed.</p>	m1 scope
Why can't you assign to an imported binding?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">// posts.js<br>export const posts = [ ... ];<br><br>// postRepo.js<br>import { posts } from &#x27;./posts.js&#x27;;<br>posts = posts.filter(p =&gt; p.id !== id);  // TypeError</pre>	<p style="text-align:left">ES module imports are <b>read-only bindings</b> — the importing file can never rebind the name. Combined with <code>export const</code>, assignment throws <code>TypeError: Assignment to constant variable.</code></p><p style="text-align:left"><b>Fix:</b> mutate the array in place instead — <code>posts.splice(index, 1)</code>, <code>posts.push(x)</code>, or <code>posts.length = 0</code>.</p>	m1 m2 modules scope
Trace the output:<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">for (var i = 0; i &lt; 3; i++) setTimeout(() =&gt; console.log(i));<br>for (let i = 0; i &lt; 3; i++) setTimeout(() =&gt; console.log(i));</pre>	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">var:  3 3 3<br>let:  0 1 2</pre><p style="text-align:left"><code>var i</code> is <b>one</b> function-scoped binding shared by all three callbacks; by the time the timers fire the loop has finished and <code>i</code> is 3. <code>let i</code> creates a <b>fresh binding per iteration</b>.</p>	m1 scope trace
<code>for...of</code> vs <code>for...in</code> on an array — what does the loop variable hold?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">for (const p of products) // p = the ELEMENT (the value)<br>for (const i in products) // i = the KEY/INDEX, as a STRING &quot;0&quot;,&quot;1&quot;</pre><p style="text-align:left"><code>for...in</code> iterates enumerable <b>keys</b>, including inherited ones. Classic bug: <code>i + 1</code> on an array index gives <code>"01"</code> because <code>i</code> is a string.</p>	m1 loops trace
Why does this return an array of <code>undefined</code>?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">arr.map(x =&gt; { x * 2 });</pre>	<p style="text-align:left">An arrow function with a <b>block body</b> <code>{ }</code> needs an explicit <code>return</code>. Without one it returns <code>undefined</code>.</p><p style="text-align:left"><b>Fixes:</b> <code>arr.map(x => x * 2)</code> (concise body, implicit return) or <code>arr.map(x => { return x * 2; })</code>.</p>	m1 functions trace
Why does this need parentheses?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">arr.map(x =&gt; ({ id: x }));</pre>	<p style="text-align:left">Without them, <code>{</code> is read as the start of a <b>function body block</b>, not an object literal, so the function returns <code>undefined</code>. Wrapping in <code>( )</code> forces it to be parsed as an expression.</p>	m1 functions
Function declaration vs function expression — which can you call before its line?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">greet();                      // works — fully hoisted<br>function greet() { }<br><br>hello();                      // ReferenceError (TDZ)<br>const hello = () =&gt; { };</pre><p style="text-align:left"><b>Declarations</b> are hoisted whole and callable earlier. <b>Expressions</b> (including arrows) are only assigned when execution reaches the line.</p>	m1 functions
What does an arrow function NOT have?	<p style="text-align:left">No own <code>this</code> (it inherits <code>this</code> lexically from the enclosing scope), no <code>arguments</code> object, and it cannot be used as a constructor with <code>new</code>.</p>	m1 functions
<code>==</code> vs <code>===</code>	<p style="text-align:left"><code>==</code> coerces types before comparing: <code>0 == "0"</code> is <code>true</code>, <code>null == undefined</code> is <code>true</code>.</p><p style="text-align:left"><code>===</code> compares type <b>and</b> value with no coercion. Default to <code>===</code>.</p>	m1 equality
Primitive types vs reference types in JavaScript	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">Primitive: String, Number, Boolean, Undefined,<br>           Null, Symbol, BigInt<br>           -&gt; copied BY VALUE<br><br>Reference: Object, Array, Function<br>           -&gt; copied BY REFERENCE (both names<br>              point at the same thing)</pre>	m1 types
What is Node.js, in the one sentence a quiz wants?	<p style="text-align:left">The V8 JavaScript engine running <b>outside the browser</b>, with an <b>event-driven, non-blocking I/O</b> model — long operations are handed off and the main thread keeps going.</p>	m2 node
What is wrong, and what is the visible symptom?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const posts = readPosts();<br>console.log(posts.length);</pre>	<p style="text-align:left">Missing <code>await</code>. <code>posts</code> is a <b>pending Promise object</b>, not the data, so <code>posts.length</code> is <code>undefined</code>.</p><p style="text-align:left"><b>Symptom to name on the quiz:</b> logging it shows <code>Promise { &lt;pending&gt; }</code>, or a property reads <code>undefined</code>.</p><p style="text-align:left"><b>Fix:</b> <code>const posts = await readPosts();</code> inside an <code>async</code> function.</p>	m2 async
What error does this produce?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">function getPosts() {<br>  const data = await fs.readFile(&#x27;p.json&#x27;, &#x27;utf8&#x27;);<br>}</pre>	<p style="text-align:left"><b>SyntaxError.</b> <code>await</code> is only legal inside an <code>async</code> function — or at the top level of an <b>ES module</b>.</p><p style="text-align:left"><b>Fix:</b> <code>async function getPosts() { ... }</code></p>	m2 async
What does an <code>async</code> function return?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">async function getCount() { return 5; }<br>const n = getCount();</pre>	<p style="text-align:left"><code>n</code> is a <b>Promise</b>, not <code>5</code>. An <code>async</code> function <i>always</i> returns a Promise; the <code>return</code> value becomes its resolved value.</p><p style="text-align:left"><b>Fix:</b> <code>const n = await getCount();</code></p>	m2 async
Why does this function return <code>undefined</code>?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">function readPosts() {<br>  fs.readFile(&#x27;posts.json&#x27;, &#x27;utf8&#x27;, (err, data) =&gt; {<br>    return JSON.parse(data);<br>  });<br>}</pre>	<p style="text-align:left">The <code>return</code> belongs to the <b>callback</b>, not to <code>readPosts</code>. <code>readPosts</code> reaches its closing brace and returns <code>undefined</code> immediately, long before the file is read.</p><p style="text-align:left"><b>Fix:</b> use <code>fs/promises</code> with <code>async/await</code>, or accept a callback parameter and invoke it with the result.</p>	m2 async callbacks
Trace the exact output:<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">console.log(&#x27;1&#x27;);<br>setTimeout(() =&gt; console.log(&#x27;2&#x27;), 0);<br>Promise.resolve().then(() =&gt; console.log(&#x27;3&#x27;));<br>console.log(&#x27;4&#x27;);</pre>	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">1<br>4<br>3<br>2</pre><p style="text-align:left">Synchronous code first (1, 4). Then <b>microtasks</b> — Promise callbacks — drain (3). Then <b>macrotasks</b> — timers and I/O (2). A <code>setTimeout(..., 0)</code> never beats a resolved Promise.</p>	m2 async trace eventloop
Callback vs Promise vs async/await — one line each.	<p style="text-align:left"><b>Callback:</b> a function passed <i>into</i> an async operation, invoked automatically when it finishes.</p><p style="text-align:left"><b>Promise:</b> an object returned <i>immediately</i> that stands in for a future result; the value is reachable only once it settles.</p><p style="text-align:left"><b>async/await:</b> syntactic sugar over Promises. <code>await</code> pauses inside an <code>async</code> function until the Promise settles and unwraps the value.</p>	m2 async
Why avoid <code>fs.readFileSync()</code> in a server route?	<p style="text-align:left">It <b>blocks the event loop</b> — no other request is served for the whole duration of the read. Use <code>await fs.readFile()</code> from <code>node:fs/promises</code>.</p>	m2 async
CommonJS vs ES Modules — syntax, enabling, and <code>__dirname</code>.	<table style="margin:auto"><tr><th>CommonJS</th><th>ES Modules</th></tr><tr><td><code>const x = require('./x.js')</code></td><td><code>import x from './x.js'</code></td></tr><tr><td><code>module.exports = x</code></td><td><code>export default x</code></td></tr><tr><td>Node default</td><td>needs <code>"type": "module"</code> or <code>.mjs</code></td></tr><tr><td><code>__dirname</code></td><td><code>import.meta.dirname</code></td></tr><tr><td>loaded at runtime, conditional OK</td><td>static, hoisted</td></tr></table>	m2 modules
What throws <code>ERR_MODULE_NOT_FOUND</code> here?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">import postRoutes from &#x27;./routes/postRoutes&#x27;;</pre>	<p style="text-align:left">The missing <b><code>.js</code> extension</b>. Node's ES module resolver requires the full filename; unlike CommonJS it does not guess.</p>	m2 modules
Why does this export nothing?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">exports = { a: 1 };</pre>	<p style="text-align:left">Reassigning <code>exports</code> breaks its link to <code>module.exports</code>, which is what Node actually reads.</p><p style="text-align:left"><b>Fix:</b> <code>module.exports = { a: 1 };</code> — or assign a property, <code>exports.a = 1;</code></p>	m2 modules
<code>dependencies</code> vs <code>devDependencies</code> — and a quiz-plausible bug.	<p style="text-align:left"><b>dependencies:</b> needed to <i>run</i> in production — <code>express</code>, <code>morgan</code>. Installed by <code>npm install pkg</code>.</p><p style="text-align:left"><b>devDependencies:</b> needed only to <i>develop</i> — <code>eslint</code>, <code>prettier</code>, <code>nodemon</code>. Installed by <code>npm install -D pkg</code>.</p><p style="text-align:left"><b>The bug:</b> <code>express</code> listed under devDependencies. <code>npm install --production</code> then skips it and the server crashes on import.</p>	m2 npm
In <code>package.json</code> scripts, which side is the name and which is the command?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">&quot;scripts&quot;: {<br>  &quot;lint&quot;: &quot;eslint src&quot;<br>}<br>//  KEY = the name you type after `npm run`<br>//  VALUE = the shell command that runs</pre><p style="text-align:left">Writing <code>"eslint src": "npm run lint"</code> defines a script <i>named</i> <code>eslint src</code> that calls itself, and <code>npm run lint</code> reports <code>Missing script: "lint"</code>.</p>	m2 npm
Why is <code>req.body</code> undefined?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.post(&#x27;/api/products&#x27;, (req, res) =&gt; {<br>  const { name } = req.body;<br>});</pre>	<p style="text-align:left">Missing the body-parsing middleware. Without it <code>req.body</code> is <code>undefined</code> and destructuring throws <code>TypeError: Cannot destructure property 'name' of 'req.body' as it is undefined.</code></p><p style="text-align:left"><b>Fix:</b> <code>app.use(express.json());</code> — registered <b>before</b> the routes.</p>	m3 express middleware
Route order: given this registration, what does <code>GET /posts/new</code> hit? What about <code>GET /posts</code>?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.get(&#x27;/posts/:id&#x27;, handlerA);<br>app.get(&#x27;/posts/new&#x27;, handlerB);<br>app.get(&#x27;/posts&#x27;,     handlerC);</pre>	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">GET /posts/new  -&gt; handlerA, with req.params.id === &quot;new&quot;<br>GET /posts      -&gt; handlerC  (NOT captured)</pre><p style="text-align:left">Express matches <b>top to bottom, first match wins</b>. <code>/posts/:id</code> shadows any <b>literal sibling at the same depth</b>, so <code>handlerB</code> is unreachable.</p><p style="text-align:left"><b>But <code>/posts</code> is safe</b> — <code>:id</code> requires a path segment to fill, and <code>/posts</code> has nothing after the slash.</p><p style="text-align:left"><b>Fix:</b> register the specific literal route <i>above</i> the parameterized one.</p>	m3 express routing
Which routes are shadowed by <code>app.get('/posts/:id', ...)</code> if it is registered first?	<p style="text-align:left"><b>Shadowed:</b> every literal one-segment sibling — <code>/posts/new</code>, <code>/posts/search</code>, <code>/posts/count</code>. Each arrives as <code>req.params.id = "new"</code> etc.</p><p style="text-align:left"><b>Not shadowed:</b> <code>/posts</code> (no segment to fill <code>:id</code>), <code>/posts/5/comments</code> (two segments, different depth), and anything on a <b>different HTTP method</b> — <code>POST /posts/new</code> is unaffected by a <code>GET</code> route.</p>	m3 express routing
<code>req.params</code> vs <code>req.query</code> vs <code>req.body</code> — source and type.	<table style="margin:auto"><tr><th></th><th>Source</th><th>Type</th></tr><tr><td><code>req.params</code></td><td>the <code>:name</code> slots in the path<br><code>/posts/5</code></td><td>always <b>string</b></td></tr><tr><td><code>req.query</code></td><td>after the <code>?</code><br><code>/posts?limit=10</code></td><td>always <b>string</b></td></tr><tr><td><code>req.body</code></td><td>the request payload<br>(needs <code>express.json()</code>)</td><td>parsed <b>JSON types</b></td></tr></table>	m3 express
Why does this never match?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const post = posts.find(p =&gt; p.id === req.params.id);</pre>	<p style="text-align:left"><code>p.id</code> is the <b>number</b> <code>5</code>; <code>req.params.id</code> is the <b>string</b> <code>"5"</code>. <code>===</code> compares type as well as value, so it is always false.</p><p style="text-align:left"><b>Fix:</b> <code>parseInt(req.params.id, 10)</code> or <code>Number(req.params.id)</code>.</p>	m3 express
What runtime error does this cause?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">if (!post) res.status(404).json({ error: &#x27;Not found&#x27; });<br>res.json(post);</pre>	<p style="text-align:left"><code>Error [ERR_HTTP_HEADERS_SENT]: Cannot set headers after they are sent to the client.</code> Both lines run — the <code>if</code> has no <code>return</code> and no <code>else</code> — and a handler may send exactly <b>one</b> response.</p><p style="text-align:left"><b>Fix:</b> <code>return res.status(404)...</code>, or wrap the second line in <code>else</code>.</p>	m3 express trace
Why does this request hang forever?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.use((req, res, next) =&gt; {<br>  console.log(req.method, req.url);<br>});</pre>	<p style="text-align:left">The middleware neither calls <code>next()</code> nor sends a response, so the request is never passed on and never answered.</p><p style="text-align:left"><b>The rule:</b> every middleware must do <b>exactly one</b> of — call <code>next()</code>, or send a response. Doing both causes the headers-sent error.</p>	m3 express middleware
What does <code>next(err)</code> do differently from <code>next()</code>?	<p style="text-align:left"><code>next()</code> continues to the next middleware in order.</p><p style="text-align:left"><code>next(err)</code> — <b>any</b> argument — skips every remaining normal middleware and jumps straight to the error-handling middleware.</p>	m3 express middleware
Why is this error handler never called?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.use((err, req, res) =&gt; {<br>  res.status(500).json({ error: err.message });<br>});</pre>	<p style="text-align:left">Express identifies an error handler <b>solely by its four parameters</b>. With three it is registered as ordinary middleware, and <code>err</code> silently receives the <i>request</i> object.</p><p style="text-align:left"><b>Fix:</b> keep all four, even if <code>next</code> is unused:</p><pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.use((err, req, res, next) =&gt; {<br>  res.status(err.status || 500).json({ errors: [err.message] });<br>});</pre>	m3 express errors
Express 4 vs Express 5: what happens to a rejected Promise in an async route handler?	<p style="text-align:left"><b>Express 4:</b> not caught. The request hangs. You must wrap the body in <code>try/catch</code> and call <code>next(err)</code>.</p><p style="text-align:left"><b>Express 5:</b> a rejected Promise from an async handler is forwarded to the error handler automatically.</p>	m3 express errors
Where must a catch-all 404 handler and the error handler be registered?	<p style="text-align:left"><b>Last</b>, after every route. Express matches top to bottom, so a catch-all registered early would swallow every request before the real routes see it.</p>	m3 express routing
Status codes: 200, 201, 204, 400, 401, 403, 404, 500 — when does each apply?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">200 OK                    successful GET / PUT<br>201 Created               successful POST that creates<br>204 No Content            successful DELETE, EMPTY body<br>400 Bad Request           client validation failed<br>401 Unauthorized          not authenticated<br>403 Forbidden             authenticated, not allowed<br>404 Not Found             resource does not exist<br>500 Internal Server Error unhandled server fault</pre>	m3 m4 http
Why is <code>res.status(204).json({ id: 5 })</code> a bug?	<p style="text-align:left">204 means <b>No Content</b> by definition, so the body is dropped and the client gets nothing. Either send <code>res.status(204).send()</code> with no body, or use <code>res.status(200).json(...)</code> if you want to return the object.</p>	m3 m4 http
Does <code>app.listen()</code> have to come last?	<p style="text-align:left"><b>Functionally, no</b> — <code>listen</code> is non-blocking, and routes registered after it still work. <b>But the expected answer</b> is: all <code>app.use</code> and routes first, <code>app.listen()</code> last. Write it that way.</p>	m3 express
RESTful URL design — state the rule and fix each of these.<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">GET  /api/getAllProducts<br>POST /api/createProduct<br>GET  /api/deleteProduct/5<br>POST /api/products/5/update</pre>	<p style="text-align:left"><b>Rule:</b> resources are <b>plural nouns</b>; the verb lives in the <b>HTTP method</b>, never the path.</p><pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">GET    /api/products<br>POST   /api/products<br>DELETE /api/products/5<br>PUT    /api/products/5</pre><p style="text-align:left">Hierarchy nests: <code>/api/posts/5/comments</code>. Filtering and paging go in the <b>query string</b>, not the path.</p>	m4 rest
Which HTTP methods are idempotent? Define the term.	<p style="text-align:left"><b>Idempotent:</b> making the identical request N times leaves the server in the same state as making it once.</p><table style="margin:auto"><tr><th>Method</th><th>Idempotent?</th><th>Safe?</th></tr><tr><td>GET</td><td>yes</td><td>yes</td></tr><tr><td>PUT</td><td>yes</td><td>no</td></tr><tr><td>DELETE</td><td>yes</td><td>no</td></tr><tr><td>POST</td><td><b>no</b></td><td>no</td></tr><tr><td>PATCH</td><td>not guaranteed</td><td>no</td></tr></table><p style="text-align:left">DELETE stays idempotent even though the 2nd call returns 404 — <b>state</b> is what must match, not the status code.</p>	m4 rest
Layered architecture: what does each layer own, and what must it never touch?	<table style="margin:auto"><tr><th>Layer</th><th>Owns</th><th>Never touches</th></tr><tr><td>Routes</td><td>URL &rarr; middleware &rarr; controller</td><td>logic, data</td></tr><tr><td>Validation MW</td><td>inspect req, send 400, stop</td><td>business rules, data</td></tr><tr><td>Controller</td><td>read req, cast types, set status, res.json()</td><td>business rules, the array</td></tr><tr><td>Service</td><td>business logic and rules</td><td><code>req</code> / <code>res</code></td></tr><tr><td>Repository</td><td>CRUD on the array</td><td><code>req</code> / <code>res</code></td></tr></table><p style="text-align:left"><b>Recite this:</b> <code>req</code> and <code>res</code> stop at the controller; the data array stops at the repository.</p>	m4 layers
Name the layering violation:<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">// services/productService.js<br>export function getProduct(req, res) {<br>  res.json(products.find(p =&gt; p.id === req.params.id));<br>}</pre>	<p style="text-align:left">The <b>service layer is touching <code>req</code> and <code>res</code></b>, and reaching into the data array directly. A service takes plain values and returns plain values.</p><p style="text-align:left"><b>Fix:</b> <code>getProduct(id)</code> calls the repository and returns the post; the controller does the <code>req</code>/<code>res</code> work.</p>	m4 layers
Name the layering violation:<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">// controllers/productController.js<br>export function getAll(req, res) {<br>  const filtered = products.filter(p =&gt; p.inStock);<br>  res.json(filtered);<br>}</pre>	<p style="text-align:left">The <b>controller is reaching into the data array</b>. Data access belongs in the repository and the filtering rule belongs in the service.</p>	m4 layers
What is wrong with this validation middleware?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">function validate(req, res, next) {<br>  if (!req.body.name) res.status(400).json({ error: &#x27;Name required&#x27; });<br>  next();<br>}</pre>	<p style="text-align:left"><code>next()</code> runs <b>even after</b> the 400 is sent, so the controller also responds and you get <code>ERR_HTTP_HEADERS_SENT</code>.</p><p style="text-align:left"><b>Fix:</b> <code>return res.status(400).json(...)</code>, and call <code>next()</code> only on the success path.</p>	m4 validation middleware
With <code>express-validator</code>, why must <code>validationResult(req)</code> live in a separate handler?	<p style="text-align:left">The <code>body()</code> / <code>param()</code> chain only <b>records</b> results onto the request — it never responds. A following handler reads them and decides:</p><pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">export function handleValidationErrors(req, res, next) {<br>  const errors = validationResult(req);<br>  if (!errors.isEmpty()) {<br>    return res.status(400).json({ errors: errors.array().map(e =&gt; e.msg) });<br>  }<br>  next();<br>}</pre>	m4 validation
Query handling: in what order do filter, sort and paginate go, and why?	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">let result = products.filter(...);              // 1. narrow<br>result = result.toSorted((a,b) =&gt; ...);         // 2. order<br>return result.slice(offset, offset + limit);    // 3. page</pre><p style="text-align:left"><b>Bug to spot:</b> paginating <i>before</i> filtering — you slice 10 rows out of the full set and then filter, so the page comes back short or empty.</p>	m4 query
What is wrong with <code>result.slice(offset, limit)</code> for pagination?	<p style="text-align:left"><code>slice</code>'s second argument is an <b>end index</b>, not a count. <code>slice(20, 10)</code> returns <code>[]</code>.</p><p style="text-align:left"><b>Fix:</b> <code>slice(offset, offset + limit)</code></p>	m4 query arrays
Why does this break on <code>?limit=</code>?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const { limit = 10 } = req.query;<br>const n = parseInt(limit, 10);</pre>	<p style="text-align:left">A destructuring default applies <b>only when the value is <code>undefined</code></b>. <code>?limit=</code> gives the empty string <code>""</code>, which is not <code>undefined</code>, so <code>limit</code> is <code>""</code> and <code>parseInt("")</code> is <code>NaN</code>.</p>	m4 query falsy
Sorting comparators — what does each line do?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">arr.sort();<br>arr.sort((a, b) =&gt; a.price - b.price);<br>arr.sort((a, b) =&gt; a.name.localeCompare(b.name));<br>arr.sort((a, b) =&gt; a.name - b.name);</pre>	<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">1. lexicographic on stringified values: [1, 10, 2, 20]<br>2. numeric ascending — correct<br>3. strings — correct<br>4. BUG: string minus string is NaN, order unchanged</pre><p style="text-align:left"><code>.sort()</code> <b>mutates</b>; <code>.toSorted()</code> returns a new array. Sorting a shared repository array in place silently reorders the stored data.</p>	m4 arrays
What is wrong with this POST handler?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">app.post(&#x27;/api/products&#x27;, (req, res) =&gt; {<br>  products.push(product);<br>  res.json(product);<br>});</pre>	<p style="text-align:left">It returns <b>200</b>; a POST that creates a resource must return <b>201 Created</b>, and the body should be the created resource <i>including its server-assigned id</i>.</p><p style="text-align:left"><b>Fix:</b> <code>res.status(201).json(product);</code></p>	m4 rest http
Why is this id generation a bug, and what are the fixes?<pre style="background:#f4f4f4;padding:6px;border-radius:4px;text-align:left">const id = products.length + 1;</pre>	<p style="text-align:left">It <b>collides after any delete</b>. Delete the last of 3 items and the next insert reuses id 3.</p><p style="text-align:left"><b>Fixes:</b> a monotonic counter (<code>let nextId = 1; ... id: nextId++</code>), or <code>Math.max(...products.map(p =&gt; p.id)) + 1</code> — the latter guarded for the empty array, since <code>Math.max()</code> of nothing is <code>-Infinity</code>.</p>	m4 rest
A short-answer question shows broken code. What three parts does a full-credit answer have?	<p style="text-align:left">1. <b>Name the defect</b> — "<code>.map()</code> returns a new array; the original is unchanged"</p><p style="text-align:left">2. <b>Say what happens at runtime</b> — "<code>prices</code> still holds the old values, so the response is wrong"</p><p style="text-align:left">3. <b>Give the fix</b> — the corrected line, written out</p>	exam strategy
<code>req.params.id</code> is what type, and what must you do with it?	A <b>string</b>. Array ids are numbers, so cast: <code>parseInt(req.params.id, 10)</code>.	recall m3
Missing <code>app.use(express.json())</code> causes what?	<code>req.body</code> is <code>undefined</code>; destructuring it throws <code>TypeError</code>.	recall m3
Missing <code>return</code> before <code>res.status(404)...</code> causes what?	<code>ERR_HTTP_HEADERS_SENT</code> — two responses from one handler.	recall m3
An Express error handler is recognized by what?	Having exactly <b>four</b> parameters: <code>(err, req, res, next)</code>.	recall m3
Every middleware must do exactly one of what two things?	Call <code>next()</code>, <b>or</b> send a response. Never both, never neither.	recall m3
<code>findIndex</code> returns what when nothing matches, and why is that dangerous?	<code>-1</code>, which is <b>truthy</b> — so <code>if (!index)</code> is always wrong. Compare <code>=== -1</code>.	recall m1 falsy
Missing <code>await</code> gives you what?	A <b>pending Promise object</b>, not the value.	recall m2
POST returns what status? DELETE?	POST &rarr; <b>201 Created</b>. DELETE &rarr; <b>204 No Content</b> with an empty body (or 200 with the deleted object).	recall m4
Query booleans: how do you test <code>?inStock=true</code>?	Compare to the <b>string</b>: <code>req.query.inStock === 'true'</code>. Never <code>Boolean(v)</code> — <code>Boolean("false")</code> is <code>true</code>.	recall m4 falsy
Which two layers must never see <code>req</code> or <code>res</code>?	The <b>service</b> and the <b>repository</b>.	recall m4 layers
